/**
 * Fond du hero : beau dégradé bleu (lueur montant du bas + voile en haut).
 * Composant serveur, pur CSS.
 */
export default function ChartBg() {
  return <div aria-hidden className="chartbg" />;
}
